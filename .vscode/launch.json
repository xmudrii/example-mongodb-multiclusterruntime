{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Debugger",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "args": [
        "--kcp-kubeconfig=kcp.kubeconfig",
        "--target-kubeconfig=cluster.kubeconfig",
      ],
    }
  ]
}
